package Day23;

public class Math类 {

    //返回最小的double 值，该值大于等于参数，并等于某个整数。
    double num1 = 3.141;
    double ceilNum = Math.ceil(num1);

    //返回最大的double 值，该值小于等于参数，并等于某个整数。
    double num2 = 3.188;
    double floor=Math.ceil(num2);

    //返回最接近参数的 long。
    double num3 = 3.1416;
    long round = Math.round(num3);

    //返回最接近参数的 int。
    float num4 = 3.14f;
    int round1 = Math.round(num4);

    //返回带正号的 double 值，该值大于等于 0.0 且小于 1.0。
    double ramdom=Math.random();










}
